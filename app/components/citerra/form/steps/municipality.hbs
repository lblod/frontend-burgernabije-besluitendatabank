<Filters::SelectFilter
  @id="municipality"
  @label="In welke gemeente ga je rijden?"
  @options={{@governmentOptions}}
  @selected={{@selectedGovernment}}
  @updateSelected={{@updateSelectedGovernment}}
  @noMatchesMessage="Geen besturen gevonden"
  @searchField="label"
  @placeholder="Kies één gemeente"
  @searchEnabled={{true}}
/>