<Filters::Filter @id={{@id}} @label={{@label}}>
    <AuInput
        id={{@id}}

        @type={{"text"}}
        @value={{@value}}
        placeholder={{@placeholder}}
        onChange={{action this.onChange}}
    />
    {{yield}}

</Filters::Filter>
