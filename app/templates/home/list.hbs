{{page-title "List"}}
<section class="au-o-flow au-u-flex au-u-flex--column" style="height: 100%;">
  {{#each this.model.items as |item|}}
    <DecisionCard
      @id={{item.id}}
      @title={{item.title}}
      @body={{item.body}}
      @approvedDate={{item.approveddate}}
      @startDate={{item.startdate}}
      @endDate={{item.enddate}}
      @municipality={{item.municipality}}
    />
  {{/each}}

</section>
<AuToolbar style="margin-top: auto;" @size="large" @nowrap={{true}}>
  <div class="au-c-pagination">

    <div class="au-c-pagination__list-item">
      <AuButton
        @skin="link"
        @icon="nav-left"
        @iconAlignment="left"
        {{on "click" this.previousPage}}
      >Vorige pagina</AuButton>
    </div>
    <p>

      <span class="au-u-hidden-visually">Resultaten</span>

      <strong>
        {{this.entriesStart}}
        -
        {{this.entriesEnd}}
      </strong>
      van 18
    </p>

    <div class="au-c-pagination__list-item">
      <AuButton
        @skin="link"
        @icon="nav-right"
        @iconAlignment="right"
        {{on "click" this.nextPage}}
      >Volgende pagina</AuButton>

    </div>
  </div>
</AuToolbar>
